{
  "name": "@canopy-mvp/sdk-node",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}